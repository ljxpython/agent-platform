# 项目结构优化



## 为什么要优化

已经完成了两个多智能体的开发,但是当前的后端代码框架还需要做适当的调整和封装,为了是后面能力的复用和好维护以及功能模块的解耦与单一

当前结构如下

![image-20250621175717672](./assets/image-20250621175717672.png)

封装大概有以下几个地方需要调整: fastapi的接口部分 数据库模块 智能体模块 以及他们之间的交互模块





## 前端优化



先把前端的模块也分门别类

```md
@/frontend 一级二级标题做如下:[
AI助力:[AI对话,测试用例生成  ] ,
ui测试 : [midscene智能体],
接口测试 : 未来提供
性能测试 : 未来提供
]

```



![image-20250621175502548](./assets/image-20250621175502548.png)



```
前端UI测试模块的前端请参考@/examples/frontend中的前端来实现,一个介绍页面一个执行页面,风格及前端功能完全参照@/examples/frontend中的代码即可
```



```
UI测试页面拆分成两个页面,页面也midscene智能系统介绍,页面二UI用例生成,点击页面1中的开始使用后,跳转到第二个页面
修复第二个页面的报错:
Failed to load resource: the server responded with a status of 401 (Unauthorized)
auth.ts:149 登出请求失败: 无效的认证凭据
logout	@	auth.ts:149
:8001/api/upload_and_analyze:1
 Failed to load resource: net::ERR_CONNECTION_REFUSED
UITestExecutePage.tsx:270 上传分析失败: TypeError: Failed to fetch
    at handleSubmit (UITestExecutePage.tsx:232:30)
UITestExecutePage.tsx:232
 POST http://localhost:8001/api/upload_and_analyze net::ERR_CONNECTION_REFUSED
UITestExecutePage.tsx:270 上传分析失败: TypeError: Failed to fetch
    at handleSubmit (UITestExecutePage.tsx:232:30)
```





```
1. 分析完成后,除了下载完整结果,前端还应该支持yaml格式脚本及playwright+midscene脚本生成
2. 每个智能体分析的结果可以点开弹窗:弹窗里有完整智能体输出内容,使用markdown进行美化
3. UI测试的界面拆分成midscene智能系统介绍和UI测试脚本生成界面
```



![image-20250621211827367](./assets/image-20250621211827367.png)上图的内容还是不美观

```
UI测试脚本生成页面一定要和之前的页面样式一样,参考: examples/frontend
当前页面不如之前的页面美观
每个智能体输出的内容也需要和之前页面样式一样能看到,弹窗内的展示可以保持当前的样子
```



```
1. Midscene生成智能体 和其他智能体的标题背景高度不一样,等待中的状态时,占了两行
2. Midscene生成智能体一直没有实时的日志输出,后端是没有问题的
3. 开始分析后,背景变成了红色,不如之前有一个圆圈美观,且颜色不好看
4. 脚本生成智能体完成后页面下方没有没出现完整结果下载,yaml及palyWright+midscene脚本下载及重新分析的按钮
```



```
1. 停止分析的背景修改为其他好看的颜色,且为浅色调
2. 页面的背景颜色修改为浅白调而非紫色
3. 查看详细结果应该出现在智能体结果的下方,而不需要下滑内容才能看到
```









## 接口及数据库优化

这一部分,最好的方式是直接给AI助手一个比较优秀的框,注意这里的前提一定是你能搞懂这个框架,知道别人写的是什么,最优解是你能够开发及写出这个框架,让AI先数据这个框架,之后在这个框架的基础上把我们的代码做调整



```
我写好了一个后端的框架@/examples/backend_examples,当前的后端需要按照该框架进行调整,使架构更加完整易懂易维护,请仔细阅读@/examples/backend_examples代码后,对@/backend目录内的代码进行优化调整
```

![image-20250621233758164](./assets/image-20250621233758164.png)



结果:

![image-20250621235813942](./assets/image-20250621235813942.png)



前端进行适配

```
@/frontend 前端的代码对接口进行适配,当前前端报错404 Not Found
```



```
参考@/examples/backend_examples/app/api/v1中的代码,实现后端的api管理,用户管理,角色管理,部门管理,之后实现前端:
对前端框架做封装,可以参考@/examples/frontend_demo中的代码,但实现上要使用本项目的前端技术栈
之后
创建一级标题系统管理:
子内容如下:
用户管理界面参考如下:

xxxx

角色管理界面参考如下:

xxxxx

api管理界面参考如下:

xxxxx

部门管理界面参考如下:
xxxx




```



```
目前前端还是没有对后端接口的内容做出正确的解析及展示,请增加详细的控制台日志用于debug,试着再次修复该问题
{
    "code": 200,
    "msg": null,
    "data": [
        {
            "id": 1,
            "name": "管理员",
            "description": "系统管理员角色",
            "is_active": true,
            "created_at": "2025-06-21T17:20:19.389406+00:00",
            "updated_at": "2025-06-21T17:20:19.389412+00:00",
            "apis": []
        }
    ],
    "total": 1,
    "page": 1,
    "page_size": 10
}
```





```
前端:/api/v1/auth/auth/logout 报错404 Not Found
/api/v1/auth/login  报错401 Unauthorized:{"error":"HTTP Exception","detail":"用户名或密码错误","status_code":401},backend/utils 中的代码请完全按照examples/backend_examples/app/utils中的实现,修复受影响的相关接口
```









```
后端服务当前缺少权限相关的管理,可以参考examples/backend_examples/app/core/dependency.py中的代码实现,examples/backend_examples/app/api/v1/__init__.py是其一个应用
```





```
将本次增加的功能放到README.md的适合位置,注意文档中工程搭建记录以上的内容不要改动
在docs中进行详细的说明,README.md给出链接到该文档
```





```
写一个文档,详细的介绍后端的这个框架,每个技术点及举例说明使用方式
```





## 智能体封装

### 思路

逻辑如下:

![image-20250622153449013](./assets/image-20250622153449013.png)

先讲其进行归类



之后按照如下思路进行整理

创建一个大模型的core,

里面有工厂类

智能体基类





```
上层接口  ->

业务流: WebOrchestrator()  -> analyze_image_to_scripts() [ 设置运行时, 智能体注册,消息机制发送 ]

大模型的base: gui-tars,qwen
各个智能体: [  图片智能体,yaml智能体,脚本执行智能体,数据库保存智能体  ]
智能体工厂: 将上述智能体注册到运行时中,assistant的创建
运行时:
每个智能体: 创建assistant, 创建team, 对运行时的消息进行处理   -> 继承一个基类
智能体记忆: 也有一个基类,可以记录一个用户,不同次对话时多个智能体的所有记忆
```







每个服务的组成

```
xxx_service:
	- 业务:业务入口,创建运行时,发布消息
	- 各个智能体,继承封装好的基类 (业务处理,存储到数据库,执行脚本)
	- 智能体,从智能体工厂创建Assistant,每个Assistant的model由core中的llm导入,实现自己的操作
	- 运行时的类: 管理消息的发布
```





### 整理

和AI对话内容如下:

```
我想要逐步优化后端的框架,请和我一起一步步优化,先完成第一步,当前完成了AI对话服务,用例生成服务,UI测试服务,当未来完成的服务更多时,在一个services文件夹下,会非常的混乱,请对backend/services 做整理,如backend/services/autogen_service.py服务为AI对话,则在services下创建一个文件夹,将AI对话服务相关的代码放在里面管理,其他服务做相关的操作
```



```
接下来我想完成框架优化的第二步,对大模型应用开发中常用组件进行封装,首先在backend文件夹下创建一个目录,名为xxx_core,将backend/core/llm.py迁移过来,先对llm进行封装,将我常用的三类llm放到llm里面,代码参考examples/frame_v1/llms.py
```



```
我已经将backend/core/llm.py删除,请将代码更新,使用最新的大模型client,将代码中废弃的client部分也都替换成新版client
```



```
接下来完成框架优化的第三步,对服务进行抽象:
以backend/services/testcase/testcase_service.py为例
抽象出智能体工厂,对应可以参考examples/frame_v1/factory.py: 创建智能体
智能体的基类可以参考examples/frame_v1/base.py,封装一些通用的能力
运行时的基类,对运行时的生命周期进行管理
memory的基类:可以记录每个用户,不同次对话时多个智能体的所有记忆
封装好的代码存放到backend/ai_core内
业务类如TestCaseService 负责和上层接口进行对接,主要调用运行时,处理业务
在上述的内容上可以进行适当的优化


```





```
backend/ai_core/testcase_runtime.py 应该放在backend/services/testcase内,将上述封装的功能运用到backend/services/testcase/testcase_service.py服务中
```



```
backend/services/testcase/agents.py 中的方法使用错误,请参考examples/frame_v1/image_analyzer.py中代码,也可以参考https://microsoft.github.io/autogen/stable/user-guide/core-user-guide/core-concepts/topic-and-subscription.html中消息订阅的用法
```





```
使用队列进行消息传递的部分需要进行适配:backend/api/v1/testcase.py的对应的适配,不应该从testcase_message_generator获取消息,在backend/ai_core/runtime.py中其实封装好了队列的消息使用方式,因此backend/services/testcase/message_collector.py文件时没有意义的,backend/services/testcase/testcase_service.py中TestCaseService直接复用即可,而非重复造轮子
```



```
对backend/services/testcase/agents.py 进行修改,1. agent中的智能体需要继承backend/ai_core/agents.py中的智能体类
2.业务逻辑及智能体的见的关系参考backend/services/testcase/testcase_service1.py,功能要和该代码中的功能对其
3. 复用backend/ai_core中已有的代码,避免重复造轮子
4. 业务代码及运行时代码在backend/services/testcase/testcase_runtime.py和backend/services/testcase/testcase_service.py
```





```
backend/services/testcase/agents.py 我不想使用backend/ai_core/agents.py中StreamingAgent,而是完全参考backend/services/testcase/testcase_service1.py中的智能体及其逻辑来实现,包括文件处理,流式输出和用户反馈,智能体memory的使用
```



```
backend/services/testcase/testcase_runtime.py的register_agents函数,有问题,比如RequirementAnalysisAgent.register(
                runtime,
                self.topic_types["requirement_analysis"],
                lambda: RequirementAnalysisAgent()
            )中RequirementAnalysisAgent() missing 1 required positional argument: 'model_client',不过我想在backend/services/testcase/agents.py中修复这个问题,RequirementAnalysisAgent的初始化不需要依赖model_client,而是在需要使用时,直接使用backend/ai_core/factory.py中create_assistant_agent来创建一个
```



```
请查阅在基类有没有create_buffered_context和get_user_memory_for_agent的类似方法,有的话请直接迁移过来

backend/ai_core/factory.py 支持传入带memory的的assistant
```



```
优化backend/ai_core/memory.py中方法,上层只需要导入就可以使用,底层增加健壮性,不需要上层再次封装一层,防止使用报错,比如下述方法async def save_to_memory(conversation_id: str, data: Dict[str, Any]) -> None:
    """保存数据到对话内存 - 使用AI核心框架的内存管理器"""
    from backend.ai_core.memory import get_memory_manager

    try:
        memory_manager = get_memory_manager()

        # 首先初始化对话内存（如果不存在）
        await memory_manager.initialize_memory(conversation_id)

        # 保存数据到内存
        await memory_manager.save_to_memory(conversation_id, data)

        logger.debug(f"💾 [Memory] 数据已保存到内存 | 对话ID: {conversation_id} | 类型: {data.get('type', 'unknown')}")

    except Exception as e:
        logger.error(f"❌ [Memory] 保存数据到内存失败 | 对话ID: {conversation_id} | 错误: {e}")
memory.py中,使用try等方法,检查是否有内存,没有则创建,其他方法也增加类似容错,之后修改backend/services/testcase/agents.py,改为导入后直接使用
```



```
ai_core中的其他代码也增加健壮性和容错机制,且每一步都有日志记录,之后检查可能受到影响的业务代码,将其修正
```



```
1.在backend/ai_core/memory.py文件中有 MessageQueue类,我希望把它独立出来和BaseRuntime进行解耦,(放在ai_core文件夹下)使用场景如下:
2. backend/services/testcase/agents.py中消息的发送使用的消息队列的方式,比如获取队列queue = testcase_service._get_message_queue(conversation_id),使用MessageQueue支持agents.py中消息队列相关能力,可以流式输出到前端,如果MessageQueue不满足,则增加其能力,注意日志输出和健壮性和容错机制
```



```
1. 替换backend/api/v1/testcase.py中的testcase_message_generator函数为backend/ai_core/message_queue.py的函数
2. backend/services/testcase/testcase_service.py中消息队列相关方法均message_queue.py中的方法,做相应的修改
3. 业务层只需要导入就可以使用,不需要上层再次封装,如果需要,请优化底层
```







```
文件处理,流式输出和用户反馈,user_memory



memory这些方法,直接调用就可以,让底层更加兼容上层这些异常
```



```
MemoryQueryEvent(source='requirement_analyst', models_usage=None, metadata={}, created_at=datetime.datetime(2025, 6, 22, 16, 50, 45, 684404, tzinfo=datetime.timezone.utc), content=[MemoryContent(content='{"type": "user_input", "content": {"conversation_id": "string", "text_content": "测试", "files": [], "round_number": 1}, "timestamp": "2025-06-23T00:49:27.870353", "conversation_id": "string", "metadata": {"stage": "requirement_analysis"}}', mime_type=<MemoryMimeType.JSON: 'application/json'>, metadata=None)], type='MemoryQueryEvent')
```



AI生成的代码有问题,做如有修改

```
backend/ai_core/message_queue.py代码中做如下修改
业务层只需要导入就可以使用,不需要上层再次封装,如果需要,请优化底层
async def get_streaming_messages_from_queue(conversation_id: str) -> AsyncGenerator[str, None]:
    """
    从独立消息队列获取流式消息生成器（全局便捷函数，用于SSE流式输出）

    Args:
        conversation_id: 对话ID

    Yields:
        str: 格式化的SSE消息
    """
    logger.info(f"🌊 [队列管理] 开始流式输出 | 对话ID: {conversation_id}")

    # 获取独立消息队列
    queue = get_message_queue(conversation_id, create_if_not_exists=True)
    if queue:
        logger.debug(f"   📦 队列获取成功，开始流式输出")
        try:
            while True:
                message = await queue.get_message(timeout=300.0)

                if message and message != "CLOSE":
                    logger.debug(f"   ✅ 消息获取成功 | 对话ID: {conversation_id}")
                    yield message
                elif message == "CLOSE":
                    logger.debug(f"   📦 队列关闭，结束流式输出 | 对话ID: {conversation_id}")
                    break
                else:
                    logger.debug(f"   ⏰ 消息获取超时 | 对话ID: {conversation_id}")
        except asyncio.CancelledError:
            logger.info(f"   ⏹️ 流式输出任务取消 | 对话ID: {conversation_id}")
        except Exception as e:
            logger.error(f"   ❌ 流式输出异常 | 对话ID: {conversation_id} | 错误: {e}")
            logger.error(f"   🐛 错误类型: {type(e).__name__}")
    else:
        logger.error(f"❌ [队列管理] 无法获取消息队列 | 对话ID: {conversation_id}")
之后修改backend/api/v1/testcase.py 受影响的部分
```



```
backend/ai_core/message_queue.py中,封装一个函数,get_streaming_sse_messages_from_queue直接返回 f"data: {message}\n\n" 格式的数据,这样backend/api/v1/testcase.py中的testcase_message_generator函数就可以直接去掉了
```



```
backend/services/testcase/agents.py 代码冗余部分优化,比如TestCaseGenerationAgent中,self._prompt 这个属性,不放在init中,写到一个函数中,需要的时候,调用函数获取
_create_assistant_agent 也是没有必要的函数,最好底层封装完成,上层导入后可以直接调用
```



```
backend/ai_core/factory.py 中create_assistant_agent 优化:兼容memory和model_context的情况,业务层只需要导入就可以使用,不需要上层再次封装
优化后,去除冗余的代码
```



```
process_user_feedback 的代码冗余,可以优化
```





```
 # 智能处理memory参数
            final_memory = memory
            if final_memory is None and auto_memory and conversation_id:
                try:
                    logger.debug(f"   🔍 自动获取对话内存 | 对话ID: {conversation_id}")
                    import asyncio
                    if asyncio.iscoroutinefunction(get_agent_memory):
                        # 如果在异步上下文中，需要特殊处理
                        try:
                            loop = asyncio.get_event_loop()
                            if loop.is_running():
                                logger.warning(f"   ⚠️ 在运行的事件循环中，无法直接调用异步函数获取内存")
                            else:
                                user_memory = loop.run_until_complete(get_agent_memory(conversation_id))
                                if user_memory:
                                    final_memory = [user_memory]
                                    logger.debug(f"   🧠 自动获取内存成功")
                        except RuntimeError:
                            logger.warning(f"   ⚠️ 无法在当前上下文中获取内存，跳过自动内存配置")
                    else:
                        user_memory = get_agent_memory(conversation_id)
                        if user_memory:
                            final_memory = [user_memory]
                            logger.debug(f"   🧠 自动获取内存成功")
                except Exception as e:
                    logger.warning(f"   ⚠️ 自动获取内存失败: {e}")

            if final_memory is not None:
                if isinstance(final_memory, list) and len(final_memory) > 0:
                    # 过滤掉None值
                    valid_memory = [m for m in final_memory if m is not None]
                    if valid_memory:
                        agent_params["memory"] = valid_memory
                        logger.debug(f"   🧠 添加内存支持，内存数量: {len(valid_memory)}")
                    else:
                        logger.debug(f"   📝 内存列表为空，跳过内存配置")
                else:
                    logger.debug(f"   📝 内存参数无效，跳过内存配置")

            # 智能处理model_context参数
            final_context = model_context
            if final_context is None and auto_context:
                try:
                    logger.debug(f"   🔧 自动创建缓冲上下文")
                    final_context = create_buffered_context(buffer_size=4000)
                    if final_context:
                        logger.debug(f"   ✅ 自动创建缓冲上下文成功")
                    else:
                        logger.debug(f"   📝 自动创建缓冲上下文失败，使用默认上下文")
                except Exception as e:
                    logger.warning(f"   ⚠️ 自动创建上下文失败: {e}")

            if final_context is not None:
                agent_params["model_context"] = final_context
                logger.debug(f"   🔧 添加模型上下文: {type(final_context).__name__}")
这段代码变得简单些,如果conversation_id存在则调用 get_agent_memory,
    create_buffered_context,
简单判断其有没有,存在update到参数中即可,不需要这样麻烦
```





```
backend/services/testcase/testcase_service.py代码中TestCaseService的process_user_feedback是冗余的
get_conversation_history 是冗余的
get_streaming_messages 是被废弃的
```







```
请和我一起进行探索,请仔细查阅分析backend/services/testcase的代码,及

我认为所有的方法都应该尽可能的解耦,让AI来帮忙看下那些功能还可以进行拆分,然后拆分后放在ai_core中


services下面有一个util_services,放一些公共可以使用的服务把document文件夹重命名


需求测试
- [需求]【Cube】 cubelinux_arm8550_common_v2.2.0.250514 版本测试
  - 两个压测场景脚本编写及压测:Node冷重启,pod开关机压测场景脚(持续测试中):https://code.byted.org/videoarch-qa/locust-stress-test/commit/6f60d86deeb534e252ac02e3ee09891cc9a9b869
  - [需求]【Cube】ipaas 支持订购时设置yaml中cpu limit字段测试完成
  - [需求]【CubeLinux】 cubelinux 内核修复补丁（net: Avoid address overwrite in kernel_connect）测试完成
自动化
- 完成cube-network NAT模块P0case:https://code.byted.org/videoarch-qa/cube-2.0/commit/e420b41cee99e32ad263b06080d8fa04aa48e338
```





```
BaseTestCaseAgent 暂时没有必要这样封装,继承RoutedAgent类可能有多个智能体,每个智能体会对应不同的提示词,这块由业务自己封装即可
在backend/ai_core/factory.py中,create_assistant_agent应该做优化兼容memory和model_context的情况,对应的可以从backend/ai_core/memory.py中获取对应的方法
业务层只需要导入就可以使用,不需要上层再次封装
对有影响的业务代码做修改,去除BaseTestCaseAgent直接使用RoutedAgent
```
