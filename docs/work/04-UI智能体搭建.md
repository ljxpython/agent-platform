成果展示



<video controls>
  <source src="./video/1750221766518-20250618_124018.mp4" type="video/mp4">
  Your browser does not support the video tag.
</video>


本次只列举提示词,有了思路和demo之后,AI在迁移和扩展方面的能力是很出众的

```
根据https://microsoft.github.io/autogen/stable/user-guide/agentchat-user-guide/tutorial/agents.html中,Multi-Modal Input一章节对backend/examples/llms.py中doubao_model_client进行封装,可以对图片进行分析
```



```
页面元素分析智能体提示词
交互分析智能体提示词
Midscene用例设计提示词

设计三个智能体,分别为页面元素分析智能体,交互分析智能体,Midscene用例生成智能体,他们的提示词在backend/examples/图片分析智能体提示词.md中
Midscene用例生成智能体会汇总页面元素分析智能体,交互分析智能体的内容,最终生成Midscene的测试脚本
后端的逻辑可以参考backend/examples/topic1.py中的逻辑,使用队列的方式传入用户的需求
```



```
我希望修改为sse流式输出的方式:几个智能体的内容都是实时的流式输出,内容放到队列中,当用户上传几张图片和需求内容后,后端开始分析且把内容流式输出,能知道是什么智能体做的输出,流式输出使用run_stream,参考
async def main_stream():
    # 获取协程对象
    result = agent.run_stream(
        task="编写一首4言古诗"
    )  # 当前代码不会执行run_stream()中的代码,直接返回协程对象
    async for item in result:
        if isinstance(item, ModelClientStreamingChunkEvent):
            print(item.content, end="", flush=True)  ## 流式输出
        if isinstance(item, TextMessage):
            print(item.content)  # 表示primary智能体最终的完整输出
        if isinstance(item, TaskResult):
            print(item.messages[0].content)  # 记录user的输入
            print(item.messages[-1].content)  # 表示primary智能体最终的完整输出
```



```
/upload_and_analyze接口的处理逻辑不对,应该是几张智能体一一分析完成,之后加上用户的需求最后给到用例生成智能体生成脚本,调用该接口后直接开始流式输出各个智能体处理的内容
```



```
生成后端对应的前端:
用户上传图片和需求,后端开始处理,流式输出,前端展示结果
```



```
三个智能体协作顺序应该为多个图片一一处理,用户需求交给交互智能体处理,最后用例生成智能体拿到两个智能体的内容后,做脚本生成
```



```
/upload_and_analyze接口的返回,应该是获取队列消息,参考backend/examples/topic1.py中的代码逻辑做改动app.get("/chat")
async def chat(user_id: str, message: str):
    asyncio.create_task(startruntime.main(user_id, message))
    return StreamingResponse(
        message_generator(user_id=user_id),
        media_type="text/plain",
        headers={
            "Cache-Control": "no-cache",
            "Connection": "keep-alive",
            "Content-Type": "text/event-stream",
        },
```





```
帮我生成对应的前端,前端可以参考backend/examples/sse_client.html,代码放在frontend目录下
```



```
前端报错CORS ,应该是跨域的问题,请解决一下
index.html:1 Access to fetch at 'http://localhost:8001/upload_and_analyze' from origin 'http://localhost:8080' has been blocked by CORS policy: No 'Access-Control-Allow-Origin' header is present on the requested resource.
app.js:155


           POST http://localhost:8001/upload_and_analyze net::ERR_FAILED 200 (OK)
handleFormSubmit @ app.js:155
(anonymous) @ app.js:34
app.js:168 上传分析失败: TypeError: Failed to fetch
    at MidsceneApp.handleFormSubmit (app.js:155:36)
    at HTMLFormElement.<anonymous> (app.js:34:18)
```



```
用例生成的智能体使用deepseek大模型,增加该model的导入,backend/examples/llms.py中的AIModel就是
增加脚本生成智能体(使用deepseek大模型):1. 提取用例生成智能体中的JSON,转化成yaml脚本和playwright脚本,在提示词中,给一个playwright的小样本参考,小样本为uitest/e2e/search.spec.ts其中uitest/e2e/fixture.ts中的内容需要导入search.spec.ts中
前端给出两种脚本的下载方式
```



```
脚本生成智能体中的提示词添加一条:Playerwright脚本头部固定添加
import { expect } from "@playwright/test";
import { test } from "./fixture";
```



```
我认为提示词这里的错误是因为python的语法中括号里的代表代码中的变量,因此报错   import { expect } from "@playwright/test";
   import { test } from "./fixture";
```








```
我完成了一个Midscene 智能体系统的系统
前端:  python3 examples/frontend/start_server.py 启动 相关代码文件在examples/llms.py  examples/midscene_agents.py
后端: python3 examples/midscene_agents.py 启动 相关代码文件夹examples/frontend
相关功能介绍: examples/README.md
我想将Midscene 智能体系统 整合到我当前的AI系统中,当前系统,后端代码在backend,前端在frontend,集成过来,使用当前系统的技术栈,阅读根目录下的README.md了解我使用的技术栈
```
